import {Container} from "react-bootstrap";

export default function NotPaid() {
    return (
        <Container className='text-center'>
            <h1>Not paid orders</h1>
        </Container>
    );
}