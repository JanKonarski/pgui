import {Container} from "react-bootstrap";

export default function NotSent() {
    return (
        <Container className='text-center'>
            <h1>Not sent orders</h1>
        </Container>
    );
}